class Solution:
    def permute(self, nums: List[int]) -> List[List[int]]:
        res = []
        def f(num, path):
            if not num: 
                res.append(path)
            else:
                for i in range(len(num)):
                    f(num[:i]+num[i+1:], path + [num[i]])
        f(nums, [])
        return res
        
-----------------------------------------------

