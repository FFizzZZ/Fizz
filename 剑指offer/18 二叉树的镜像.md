```
class Solution:
    def Mirror(self, root):
        # write code here
        if root:
            temp = root.left
            root.left = self.Mirror(root.right)
            root.right = self.Mirror(temp)
        return root
```
