## 翻转两次
```
class Solution:
    def ReverseSentence(self, s):
        # write code here
        s = s[::-1]
        ans = ''
        temp = ''
        for i in s:
            if i != ' ':
                temp += i
            else:
                ans += temp[::-1] + ' '
                temp = ''
        ans += temp[::-1]
        return ans
```

## 也可以用栈存储单词，然后出栈。


## 内置函数
```
class Solution:
    def ReverseSentence(self, s):
        return " ".join(s.split()[::-1]) if s.strip() != "" else s
```
